/** Popover width, and the gap between it and the counts it hangs off. */
export const POPOVER_WIDTH = 360;
export const POPOVER_GAP = 8;

/** Below this much room under the cell, the popover opens upward instead. */
export const POPOVER_FLIP_BELOW = 340;

/** Grace period for the pointer to cross the gap into the popover. */
export const CLOSE_DELAY_MS = 120;
