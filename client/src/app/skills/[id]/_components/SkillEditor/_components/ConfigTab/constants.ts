/** A skill body is Markdown; the editor header shows it as `<name>.md`. */
export const BODY_FILE_EXT = ".md";

/** Fallback filename when the name field is empty mid-edit. */
export const UNNAMED_FILE = "skill";
