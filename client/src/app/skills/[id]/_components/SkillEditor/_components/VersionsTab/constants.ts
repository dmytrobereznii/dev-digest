/** Rows rendered by the loading skeleton before the history arrives. */
export const SKELETON_ROWS = 3;

/** Height of one skeleton row, matching a rendered version row. */
export const SKELETON_ROW_HEIGHT = 52;
